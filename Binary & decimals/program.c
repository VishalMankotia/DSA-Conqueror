#include <stdio.h>

int main()
{
    int num;
    char s;

    scanf("%d", &num);
    scanf("%s", &s); // Reading input from STDIN
    printf("%d\n", num * 2);
    printf("%s\n", s); // Writing output to STDOUT
}
